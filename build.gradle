buildscript {
    ext {
        groupName = "entrnce"
        applicationVersion = '2.36.0-SNAPSHOT'
    }
}
plugins {
    id "java"
}

allprojects {
    group = "${groupName}"
    version = "${applicationVersion}"
}

buildscript {
    ext {
        groupName = "entrnce"
        applicationVersion = '2.35.0'
    }
}
plugins {
    id "java"
}

allprojects {
    group = "${groupName}"
    version = "${applicationVersion}"
}
